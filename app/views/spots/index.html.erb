<h1>投稿一覧</h1>

<% @spots.each do |spot| %>

  <p>ユーザー:<%= spot.user.name %></p>

  <p>人物:<%= spot.person %></p>
  
  <p>場所:<%= spot.address %></p>

  <p>タイトル:<%= link_to spot.title, spot_path(spot.id) %></p>

<% end %>
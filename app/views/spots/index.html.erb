<h1>投稿一覧</h1>

<% @spots.each do |spot| %>

  <p>人物</p>
  <span><%= spot.person %></span>
  
  <p>場所</p>
  <span><%= spot.address %></span>

  <p>タイトル</p>
  <span><%= link_to spot.title, spot_path(spot.id) %></span>

<% end %>
<h1>いいね一覧</h1>

<table class="table">
    <tbody>
      <% @favorite_spots.each do |spot| %>
        <tr>
          <td>
            <%= link_to spot.title, spot_path(@spot) %>
          </td>
        </tr>
      <% end %>
    </tbody>
</table>